/*
= (single equals) is the "assignment operator" (how we store values in variables).
Assignment process goes from RIGHT TO LEFT: everything to the right of the = is
resolved, THEN assigned to the variable name on the left.
*/

// Setup
var a;
var b = 2;

// Only change code below this line
a = 7;
b = a;
