/*
When variables are declared (and not initialized to a starting value), they have the
value/data type of undefined. Mathematical operations on undefined variables result
in NaN ("Not a Number"); string concatenations with undefined variables result in the
string literal "undefined".
*/

// Initialize these three variables
var a = 5;
var b = 10;
var c = "I am a";

// Do not change code below this line
a = a + 1;
b = b + 5;
c = c + " String!";
