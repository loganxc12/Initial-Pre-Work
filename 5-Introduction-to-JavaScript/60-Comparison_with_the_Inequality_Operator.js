/*
The inequality/"Not Equal" operator (!=) is the opposite of the equality operator;
Like the equality operator, the inequality operator will convert data types.
*/

// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);
