/*
The REMAINDER (sometimes incorrectly called the "modulus") operator (%) gives the
remainder of the division of two numbers; can be used to check whether a number is
even or odd.
*/

var remainder = 11 % 3;
