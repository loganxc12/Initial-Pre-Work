/*
All comparison operators return a boolean (true/false). We can leverage this to write
more succinct code and avoid chaining long if/else statements.
*/

function isLess(a, b) {
  // Fix this code
  return a < b;
}

// Change these values to test
isLess(10, 15);
